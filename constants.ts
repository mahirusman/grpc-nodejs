import path from "path";
export const constants = {
  HOST: "0.0.0.0",
  PORT: 3000,
  PROTO_FILE: `${__dirname}/proto/todo.proto}`,
};

// console.log("__dirname", path.resolve(`${__dirname}/proto/todo.proto}`));
